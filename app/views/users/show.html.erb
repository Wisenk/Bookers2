<div>
  <h3><%= @user.name %></h3>
  <%= image_tag @user.get_profile_image(100, 100) %>
</div>

<h2>投稿一覧</h2>
<% @user.books.each do |book| %>
  <div>
    <h3><%= book.title %></h3>
    <p><%= book.body %></p>

  </div>
<% end %>